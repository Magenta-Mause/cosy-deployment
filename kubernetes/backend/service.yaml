apiVersion: v1
kind: Service
metadata:
  name: cosy-backend
  namespace: cosy
spec:
  selector:
    app: cosy-backend
  ports:
    - port: 80        # inside the cluster
      targetPort: 8080
      protocol: TCP
  type: ClusterIP

